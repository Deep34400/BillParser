# ─── Server ───────────────────────────────────────────────────────────────────
PORT=4000
NODE_ENV=development
LOCAL_DEV=true

# ─── GCP / Firestore ─────────────────────────────────────────────────────────
GCP_PROJECT_ID=billparser-dev
STORAGE_BUCKET=billparser-uploads
FIRESTORE_PREFIX=
FIRESTORE_DATABASE_ID=(default)

# ─── OCR Providers ────────────────────────────────────────────────────────────
MISTRAL_API_KEY=
MISTRAL_MODEL=mistral-small-latest
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
VERTEX_LOCATION=global

# ─── Auth ─────────────────────────────────────────────────────────────────────
ADMIN_EMAIL=admin@praya.io
ADMIN_PASSWORD=admin123

# ─── Email Intake (IMAP polling for invoice attachments) ──────────────────────
# Set IMAP_ENABLED=true to activate email polling on server start.
IMAP_ENABLED=false
IMAP_HOST=imap.gmail.com
IMAP_PORT=993
IMAP_USER=techcarrum@gmail.com
IMAP_PASSWORD=hfqi amsf sctt gpaa
POLL_INTERVAL_SEC=90

# ─── Other ────────────────────────────────────────────────────────────────────
SIGNED_URL_TTL_MINUTES=60
BUYER_GSTIN_BLOCKLIST=
